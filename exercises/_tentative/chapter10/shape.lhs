* `flatten :: Tree -> [Int]` is defined on page 104 (ja: 125)
* define the reverse: a function `shape` that creates a Tree from a list, in
  the shape of an existing tree

> shape :: [Int] -> Tree -> Tree
